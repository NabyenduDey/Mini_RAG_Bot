# Python 3.10–3.12 recommended. PyTorch has no macOS x86_64 wheel for Python 3.13 yet,
# so `pip install torch` fails with "No matching distribution" on 3.13.
# If pip uses a private index that omits torch, force PyPI, e.g.:
#   pip install -r requirements.txt --index-url https://pypi.org/simple

python-telegram-bot>=21.0
httpx>=0.27.0
# Transformers 5.x requires torch>=2.4; PyPI torch on macOS x86_64 is often max ~2.2.x, which
# makes transformers treat torch as unavailable and crash (e.g. LRScheduler NameError). Stay on 4.x.
sentence-transformers>=3.0.0,<5
numpy>=1.24.0,<2
sqlite-vec>=0.1.6
# Prebuilt SQLite with extension loading (stdlib sqlite3 often lacks this on macOS).
pysqlite-binary>=0.5.1
python-dotenv>=1.0.0
Pillow>=10.0.0

# Local image → text (BLIP, LLaVA, …)
transformers>=4.44.0,<5
accelerate>=0.27.0
torch

# Optional backends (uncomment or pip install separately):
# pytesseract>=0.3.10   # only if IMAGE_TEXT_BACKEND=tesseract
# clip-interrogator     # only if IMAGE_TEXT_BACKEND=clip_interrogator
